---
framework: jest
postMerge:
  pattern:
    - 'test/development/**/*.test.{js,ts,tsx}'
preMerge:
  pattern:
    - 'test/development/**/*.test.{js,ts,tsx}'
preRun:
  command:
    - yarn
postRun:
  command:
    - node --version
version: 2.0
